requests==2.34.2
urllib3==2.7.0
Pillow==12.2.0
openpyxl==3.1.5
customtkinter==6.0.0
python-dotenv==1.2.2

# v2.0 Fleet Picker -- live Snowflake mode. The [secure-local-storage] extra
# pulls keyring so the SSO token is cached (no re-prompt every launch).
# truststore makes TLS verify against the OS/corporate cert store, the same
# self-signed-CA wall that needs setup.bat's --use-feature=truststore fallback.
snowflake-connector-python[secure-local-storage]==4.7.1
truststore==0.10.4
